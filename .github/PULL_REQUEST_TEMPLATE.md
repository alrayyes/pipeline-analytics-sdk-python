## Summary

<!-- What changed and why, in a sentence or two. -->

## Test plan

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check .` and `uv run ruff format --check .` are clean
- [ ] `uv run mypy .` is clean
- [ ] `uv run mutmut run` has no surviving mutants
